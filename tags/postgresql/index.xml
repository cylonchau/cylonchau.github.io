<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>postgresql on Cylon&#39;s Collection</title>
    <link>https://www.oomkill.com/tags/postgresql/</link>
    <description>Recent content in postgresql on Cylon&#39;s Collection</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>zh-cn</language>
    <lastBuildDate>Mon, 05 Oct 2020 00:00:00 +0000</lastBuildDate><atom:link href="https://www.oomkill.com/tags/postgresql/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>Docker运行PostgreSQL</title>
      <link>https://www.oomkill.com/2020/10/postgresql-docker-setup/</link>
      <pubDate>Mon, 05 Oct 2020 00:00:00 +0000</pubDate>
      
      <guid>https://www.oomkill.com/2020/10/postgresql-docker-setup/</guid>
      <description></description>
      <content:encoded><![CDATA[<blockquote>
<p>本篇文章发布于<a href="https://cylonchau.github.io/">cylon的收藏册</a>，转载请声明出处哦~</p>
</blockquote>
<p>在本文，尝试使用 Docker 运行 PostgreSQL ，为了适配 goalert 项目，因为从来没有尝试过使用 PostgreSQL</p>
<h2 id="了解postgresql数据库">了解PostgreSQL数据库</h2>
<p>在我们继续运行 PostgreSQL 数据库的 Docker 容器之前，我们先来了解一下 PostgreSQL 数据库。   <strong>PostgreSQL 是一个开源 RDMS，类似于 MySQL。   它是一个面向对象的数据库，但我们可以处理结构化和非结构化数据。</strong></p>
<p>PostgreSQL 数据库可以运行在各种平台上，包括 Windows、Mac OS X 和 Linux。它还提供高级数据类型和性能优化功能来存储和扩展复杂的数据库工作负载。</p>
<h2 id="使用公共镜像运行postgresql">使用公共镜像运行PostgreSQL</h2>
<p>要使用 Docker 运行 PostgreSQL，我们首先需要拉取 <a href="https://hub.docker.com/postgres">Docker Hub </a> 上可用的 postgres 公共镜像：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">docker pull postgres
</span></span></code></pre></td></tr></table>
</div>
</div><p>在上面的命令中，我们拉取了 <em>postgres</em> 最新的稳定版镜像。 如果要指定版本的 <em>postgres</em> 镜像，可以使用以下命令</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">docker pull postgres:14.2
</span></span></code></pre></td></tr></table>
</div>
</div><p>这里将使用 <em>postgres:14.2</em>  版本来运行 Postgres 的容器，这里命令主要为 Linux</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">docker run -d -e <span class="nv">POSTGRES_USER</span><span class="o">=</span>admin -e <span class="nv">POSTGRES_PASSWORD</span><span class="o">=</span><span class="m">111111</span> -p 5432:5432 -v /data:/var/lib/postgresql/data --name postgresql postgres
</span></span></code></pre></td></tr></table>
</div>
</div><p>如果在 window 或 wsl 上运行，可以执行下面命令</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">docker run -d -e <span class="nv">POSTGRES_USER</span><span class="o">=</span>admin -e <span class="nv">POSTGRES_PASSWORD</span><span class="o">=</span><span class="m">111111</span> -p 5432:5432 -v /data:/var/lib/postgresql/data --name postgresql postgres
</span></span></code></pre></td></tr></table>
</div>
</div>]]></content:encoded>
    </item>
    
  </channel>
</rss>
